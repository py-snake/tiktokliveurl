<?php

// Copy this file to config.php and set your own secret token.
// config.php is gitignored, so never commit your real token.

return [
    // Master token - change this to your own secret token
    'master_token' => 'CHANGE_ME_TO_A_RANDOM_SECRET',

    // TikTok API settings
    'tiktok' => [
        'webcast_url' => 'https://webcast.tiktok.com',
        'web_url' => 'https://www.tiktok.com',
        'user_agent' => 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/126.0.6478.127 Safari/537.36',
        'timeout' => 10,
    ],

    // Proxy settings (leave empty for no proxy)
    'proxy' => '', // e.g., 'http://user:pass@host:port'

    // SSL verification (set true in production with proper CA bundle)
    'ssl' => [
        'verify_peer' => false,
        'verify_host' => false,
        'ca_bundle' => '', // path to CA bundle if verify_peer=true
    ],

    // CORS settings
    'cors' => [
        'enabled' => false,
        'origins' => ['*'],
        'allow_credentials' => false,
    ],

    // Chat / WebSocket signing (EulerStream)
    // For live comments: browser JS connects outbound wss://, PHP mints
    // the signed URL via this sign server. Free tier works without a key
    // but is heavily rate-limited (~10/day/IP). Set SIGN_API_KEY for
    // production or self-host via SIGN_API_URL.
    'chat' => [
        'sign_api_key' => '', // or env SIGN_API_KEY
        'sign_api_base' => 'https://tiktok.eulerstream.com', // or env SIGN_API_URL
    ],
];
